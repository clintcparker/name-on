# Release Record: Working Short Install URLs

**Feature**: `002-publish-install-scripts`
**Shipped**: 2026-07-30T01:01:41Z
**Pipeline**: `/speckit.ship`

## Pull Request

| | |
|---|---|
| **PR** | [#18 — feat: Working Short Install URLs — the install URL the CLI advertises now resolves](https://github.com/clintcparker/name-on/pull/18) |
| **Branch** | `002-publish-install-scripts` → `main` |
| **Remote** | `origin` (`git@github.com:clintcparker/name-on.git`) |
| **Commit range** | `1fcfeb0e8e8538ceff9c686dbe0babd8e19db475..56046da27fdfc4ee87dab134f04ebf23c1a67aff` |
| **Commits** | 1 |
| **Diff** | 17 files, +1731 / -6 |
| **Mergeable** | MERGEABLE (0 commits behind `main` at ship time; no rebase performed) |
| **PR description** | [`pr-description-20260730T010141Z.md`](./pr-description-20260730T010141Z.md) |

## Readiness check results

| Gate | Result | Detail |
|---|---|---|
| Tasks | ⚡️ 23/25 | T024, T025 are labeled post-merge in `tasks.md` and cannot run before the Azure SWA deploy. Confirmed by the maintainer as expected. |
| Review report | ➖ none | No `reviews/` directory — `/speckit.review` was not run for this feature. |
| QA report | ➖ none | No `qa/` directory — `/speckit.qa` was not run for this feature. |
| Local tests | ✅ 81/81 | `dotnet test name-on.sln` — 42 `name-on-cli-tests` (6 new), 39 `name-on-unit-tests`. |
| Publish artifact | ✅ | Both installers present in `build_output/wwwroot/` and byte-identical to `install/`; `staticwebapp.config.json` present; `grep -c navigationFallback` → 0. |
| Single-source (FR-007) | ✅ | `git ls-files '*install.sh' '*install.ps1'` → exactly `install/install.sh`, `install/install.ps1`. |
| Installers unmodified (FR-011) | ✅ | No change under `install/`. |
| Follow-up filed | ✅ | Issue [#17](https://github.com/clintcparker/name-on/issues/17) — releases carry no CLI binaries (T022). |
| Working tree | ❌→✅ | Started dirty with the branch **0 commits ahead**; all feature work was uncommitted. Resolved by a single squashed feature commit at the maintainer's direction. |
| Branch sync | ✅ | 0 behind, 0 ahead of `origin/main` before the commit. No rebase or merge was required or performed. |

**Verdict at ship time**: READY TO SHIP (with notes) — shipped.

## CI

No CI run existed on this branch before shipping: the branch had no commits, and
the deploy workflow triggers on `push: main` / `pull_request → main`, not on a
feature-branch push. The first run is therefore the PR itself:

- `Build and Deploy Job` — [run 30504547506](https://github.com/clintcparker/name-on/actions/runs/30504547506) (pending at time of writing)
- `Close Pull Request Job` — skipped (only runs on PR close)

`verify-install-urls.yml` does **not** run on this PR by design — it triggers on
`workflow_run` completion of the deploy workflow, on a daily schedule, and on
`workflow_dispatch`. Its first meaningful run is after the post-merge deploy to
production (T025).

## Changelog entry

Added to a newly created root [`CHANGELOG.md`](../../../CHANGELOG.md) under
`[Unreleased]`, as `Working Short Install URLs — 2026-07-29`:

- **Added** — site serves both installers at `/install.sh` and `/install.ps1`;
  PowerShell one-liner in help output; `staticwebapp.config.json`;
  `verify-install-urls.yml`; `InstallAdvertisementTests`.
- **Changed** — help `Install:` section lists four channels with aligned labels;
  help text extracted to `Program.HelpText`.
- **Fixed** — `https://name-on.clintcparker.com/install.sh` returned 404 despite
  being the command the CLI printed.
- **Technical notes** — referenced-not-copied publication; deliberate absence of
  `navigationFallback`; the dev server cannot serve these assets (validate against
  publish output or production); installers themselves unchanged; known limitation
  #17.

## Traceability

| Artifact | Path |
|---|---|
| Specification | [`spec.md`](../spec.md) |
| Implementation plan | [`plan.md`](../plan.md) |
| Research (R1–R9, empirical) | [`research.md`](../research.md) |
| Data model | [`data-model.md`](../data-model.md) |
| HTTP contract | [`contracts/http-installer-endpoints.md`](../contracts/http-installer-endpoints.md) |
| CLI help contract | [`contracts/cli-help-install-section.md`](../contracts/cli-help-install-section.md) |
| Validation guide | [`quickstart.md`](../quickstart.md) |
| Task list + verification record | [`tasks.md`](../tasks.md) |
| Requirements checklist | [`checklists/requirements.md`](../checklists/requirements.md) |
| Review report | — not produced |
| QA report | — not produced |

## Outstanding after merge

1. **T024** — run [`quickstart.md`](../quickstart.md) §6 against the deployed
   site: `200` + `text/plain` for both installer URLs, byte-identity via `diff`,
   browser renders without downloading (SC-003), `404` for `/Install.sh`,
   `/install.sh/`, `/install.txt`, `/does-not-exist-xyz`, and `301` to `https://`
   for the insecure-scheme request.
2. **T025** — `gh workflow run verify-install-urls.yml && gh run watch` green
   (quickstart §9), then prove it detects a break by advertising a bogus installer
   URL on a scratch branch and confirming the job fails (FR-009, SC-005).
3. **Issue [#17](https://github.com/clintcparker/name-on/issues/17)** — until a
   release carries the `name-on-<rid>` archives, an install run still fails at the
   binary-download step from *any* installer URL, including the README's. **SC-001
   remains unmet** and is not attributable to this feature.
