{
  "version": "0.2.0",
  "configurations": [
    {
      "name": ".NET Core Launch (Blazor WASM)",
      "type": "blazorwasm",
      "request": "launch",
      "preLaunchTask": "build (blazor wasm)",
      "url": "http://localhost:5000",
      "cwd": "${workspaceFolder}/name-on-blazor",
      "browser": "edge"
    }
  ]
}
